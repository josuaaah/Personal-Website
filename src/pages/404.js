import * as React from "react"

import Layout from '../components/layout';

const NotFoundPage = () => {
  return (
    <Layout>
      <p class="lead">This page is empty... for now. In the meantime, how about we get some coffee?</p>
    </Layout>
  )
}

export default NotFoundPage